# 对 Claude 独立评审的回应与修复状态

- 回应日期：2026-07-12
- 对应报告：`eval/REVIEW_REPORT.md`
- 状态口径：`已修复` = 已修改并完成针对性验证；`部分修复` = 已改进但原问题范围尚未全部关闭；`未修复` = 保留问题并说明原因。
- 本文件不改写 Claude 的原始结论；下一轮复审应以当前工作区源码和本表为准重新采证。

## 逐项回应

| # | Claude 问题/建议 | 回应 | 状态 | 验证或剩余工作 |
| ---: | --- | --- | --- | --- |
| 1 | `coulomb.html` 库仑力单位错误 | 接受。改用 `k0=8.99e9 N·m²/C²`，将界面的 μC 显式换算为 C；力、`k0|q1q2|`、画布公式和 F-r 曲线统一使用 SI 计算。为保持运动可观察，将演示物体明确为 8 g 轻质带电小球，没有再对物理力值做倍率伪装。 | **已修复** | 默认值浏览器实测为 `5.99×10⁻³ N`；页面无 JS 错误。 |
| 2 | `electric-field.html` 电场强度单位错误 | 接受。改用 SI 库仑常量并将 μC 换算成 C；读数与画布公式统一用科学计数法。 | **已修复** | 默认值浏览器实测为 `4.99×10³ N/C`；页面无 JS 错误。 |
| 3 | `bx1.md` 的 done 徽章与 `progress.json` 的 review 冲突 | 接受。当前 32 个硬编码徽章全部同步为 `review`，与账本及 README 口径一致。 | **已修复** | `bx1.md` 中 32 个状态均为 `status review`，不再存在 `status done`。长期方案仍可改成运行时单一数据源，但不影响本次一致性关闭。 |
| 4 | `isPublishableProblem` 误杀无数字的符号计算题 | 接受。保留严格过滤，新增显式 `symbolic: true` 豁免；已在 GT-2025-SD-15 的正式数据和导入批次中同步标注。 | **已修复** | 仓库严格检查通过；该题不再因“无数字”被过滤。 |
| 5 | 章节公式使用反引号，KaTeX 已加载但未使用 | 接受问题，但不接受用正则一次性转换 1800+ 处公式作为安全修复。公式语义、中文单位、行内代码和上下标需要逐章迁移及渲染复核。 | **未修复** | 后续应按章节分批迁移，每批执行内容检查和视觉抽查；迁移完成前保留 KaTeX 资源，因此“白加载”问题也保持开放。 |
| 6 | 知识图谱 226 节点仅 48 条边 | 接受。未机械添加“同章相邻即前置”边，因为章节顺序不等于严格先修关系，自动补边可能制造错误教学关系。 | **未修复** | 需要先定义 prerequisite/application/model 的审核规则，再由物理内容复核逐批补边。 |
| 7 | 13 个交互页缺观察任务 | 接受。为 capacitor-lab、charges-field、dc-circuit、field-lines、double-slit、refraction、mutual-induction、model-selector 和 5 个 skill 页面补入明确的“先操作—再比较—验证规律”任务。 | **已修复** | 13/13 页面源码均能检出“观察任务”。 |
| 8 | 播放、暂停、重置语义不统一 | 接受，但该项涉及多页交互契约，不能只改按钮文案。 | **未修复** | 后续需先列出所有重置按钮的目标状态，再统一为“全部重置”或拆分“重置播放/全部重置”，并做逐页重入测试。 |
| 9 | 纯颜色指代不利于色觉障碍用户 | 接受。已把 Claude 点名的 braking 错误外推线改为“颜色 + 虚线 + 错误外推标签”三重编码；charges-field、field-lines 的正负电荷也增加 `+/−` 文字编码。 | **部分修复** | 全站仍有若干“蓝色箭头/红色手柄”描述，需要结合画面标签逐页检查，不能仅靠文本替换关闭。 |
| 10 | plank-block 的 `aM=μmg/M` 未写地面光滑前提 | 接受。交互页公式卡和 `bx1.md` 均补充“地面光滑”前提。 | **已修复** | 源码检查确认公式与前提同屏/同段出现。 |
| 11 | iframe 固定高度，建议 postMessage 自适应 | 建议合理，但属于站点壳层协议升级，需同时修改 203 个子页或提供统一注入机制。 | **未修复** | 当前 390 px 宽度测试无横向溢出；高度自适应另立任务实现并回归路由、打印和移动端滚动。 |
| 12 | 将 eval 静态评估接入 `npm run check` | 接受方向，但当前 `eval/static-eval.mjs` 仍报告 903 个公式 P3 和 1 个已确认的缺图误报，且会写入结果文件，不适合直接成为阻断式检查。 | **未修复** | 应先让 eval 支持只读/非写入模式、消除已知误报并定义 P3 是否阻断，再接入主检查。 |

## 额外回应

- Claude 关于两个 P1 的判断正确，属于此前审核遗漏，本次已按 SI 量纲修复。
- “203 页物理内核及 231 道题全部无误”是 Claude 评审报告的结论；本轮没有重新宣称或扩大该结论，只验证本次修改直接影响的页面与仓库检查。
- 原评审报告和原问题清单保留不动；修复状态以本文件记录，等待 Claude 重新采证后更新其结论。

## 本轮验证

1. `npm run check`：通过；仅保留不阻断的隔离旧数据清理警告。
2. `node eval/static-eval.mjs`：完成，203 页/13 章节；仍有 903 个 P3（主要为公式规范）和 1 个报告已确认的缺图误报。
3. 真实 Chromium 针对性检查：
   - `anim/bx3/coulomb.html`：默认 `F=5.99×10⁻³ N`，无 page error。
   - `anim/bx3/electric-field.html`：默认 `E=4.99×10³ N/C`，无 page error。

## 汇总

- 已修复：6 项（#1、#2、#3、#4、#7、#10）
- 部分修复：1 项（#9）
- 未修复并保留：5 项（#5、#6、#8、#11、#12）

