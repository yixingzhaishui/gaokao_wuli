# 当前连续开发检查点

更新时间：2026-07-12

## 任务状态

- `T01`—`T07`：已完成；`T08`：进行中，等待最终发布版验收。
- 已完成的旧模块报告只能作为覆盖清单，不能再单独证明滑块、单选或下拉控件可用：旧审核器会直接写入 DOM 值。
- 本轮已用真实浏览器输入完成 B3 25 页、XB2 22 页、XB1 21 页、BX2 27 页、BX1 32 页、EXP 26 页、MODEL 3 页、SKILL 11 页和 BX3 36 页全模块复核，共 `203/203 PASS`、`0 BLOCKED`；完整列表见 `audit/T07_REAL_CONTROL_AUDIT.md`。

## 最近真实提交

`2c478ed test: exercise select controls through browser input`

## 本轮已验证的发布/浏览器路径

- 发布版库仑定律：播放后 `r: 3.00 → 3.52`、`F: 6.00 → 4.37`、`v: 0.00 → 0.81`；“距离加倍”“异号相吸”“重置”均改变并恢复预期状态，且无控制台错误。
- B3-01 电荷与起电：真实范围拖拽、全部可见模式按钮、播放与重置均通过；进入静电感应后，“接地 → 移开带电体”两个动态出现的步骤都实际改变画面或物理读数。
- 串并联电路：在真实 Chromium 鼠标拖拽及 390×844 触摸配置中，R1、R2、U 均可改变；R1=12Ω、R2=1Ω、U=24V 时串联读数为 `R=13Ω, I=1.85A, U1=22.2V, U2=1.8V`，切换并联后为 `R≈0.92Ω, I=26A, I1=2A, I2=24A`。
- 内置浏览器的原生 range 交互不可靠，曾显示假失败；独立 Chromium 鼠标/触摸复现用于最终判定。
- XB2 洛伦兹力的 B 滑块真实故障已修：错误的回写键名会让手柄跳回；现已用真实拖拽复测通过。圆周运动页的端点 α 滑块也已在修正后的真实拖拽门禁下通过。电能传输页重置漂移已修，重置后稳定恢复 100kW、10kV、10Ω。

## 当前审核门禁

1. 范围滑块必须由真实拖拽触发，不得写入 `input.value` 后伪造事件。
2. 下拉、复选和单选必须通过浏览器交互改变。
3. 动态出现的按钮必须进入对应情境后逐一执行；B3-01 的感应分步流程已成为范例硬门禁。
4. 任何旧报告中缺少上述证据的页面都为 `IN_PROGRESS`，不能以旧分数关闭。

## 下一条具体动作

对推送后的 Pages 分支做最终发布验收：手机目录跳转、图谱静止/拖拽、库仑定律、串并联电路、综合专题代表页；随后运行严格检查、提交并推送本批修复与账本。绝不混入题库资料。
